{
  "name": "@enigma/map-gateway",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "tsc --noEmit",
    "dev": "wrangler dev",
    "dataset:validate": "node scripts/validate-dataset.mjs",
    "test": "vitest",
    "test:run": "vitest run",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260819.1",
    "typescript": "catalog:",
    "vitest": "catalog:",
    "wrangler": "4.124.0"
  }
}
